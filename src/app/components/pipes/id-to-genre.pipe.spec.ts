import { IdToGenrePipe } from './id-to-genre.pipe';

describe('IdToGenrePipe', () => {
  it('create an instance', () => {
    const pipe = new IdToGenrePipe();
    expect(pipe).toBeTruthy();
  });
});
