import { CardGameDirective } from './card-game.directive';

describe('CardGameDirective', () => {
  it('should create an instance', () => {
    const directive = new CardGameDirective();
    expect(directive).toBeTruthy();
  });
});
