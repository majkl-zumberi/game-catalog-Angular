import { PriceGameDirective } from './price-game.directive';

describe('PriceGameDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceGameDirective();
    expect(directive).toBeTruthy();
  });
});
